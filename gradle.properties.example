# Copy these lines into ~/.gradle/gradle.properties and fill in
# your own values. The PAT needs the 'read:packages' scope to pull
# karoo-ext from GitHub Packages.
gpr.user=YOUR_GITHUB_USERNAME
gpr.key=
